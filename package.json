
{
	"name": "phaser3-vite-template",
  "description": "Notes -@vitejs/plugin-react is required for replit as we don't have access to vite.config.js",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc && vite build",
		"preview": "vite preview"
	},
	"devDependencies": {
		"eslint": "^8.25.0",
    "typescript": "^4.7.4",
		"vite": "^3.1.0"
    "@vitejs/plugin-react": "^2.0.0",
	},
	"dependencies": {
		"phaser": "^3.55.2"
	}
}